import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.modbina.app",
  appName: "مود بناء",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
